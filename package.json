{
  "name": "ultra-cosmos",
  "version": "0.1.0",
  "private": true,
  "description": "ULTRA COSMOS — open-world space exploration sandbox (vertical slice). Runs in the browser with Three.js, no build step.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -c-1",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/e2e/smoke.mjs"
  },
  "devDependencies": {
    "three": "0.170.0"
  },
  "license": "UNLICENSED"
}
